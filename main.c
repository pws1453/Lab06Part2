#include <stdio.h>

void hashLoop(){
    int status;
    char* command;
    do{
        printf("hash>>");
        scanf("%s",command);
    }while (status);
}

int main(int argc,char *argv[]) {
    printf("Hello, World!\n");
    return 0;
}
